#!/usr/bin/env fontforge
if (Strtol($version) < 20090622)
  Error("Please upgrade to fontforge 20090622 or later version")
endif

filename = $1:r
Open(filename + ".sfd")

Print("Generating " + filename + ".otf...")
Generate(filename + ".otf")
Print("Done")
